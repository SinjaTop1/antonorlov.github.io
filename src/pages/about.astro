---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead title={`CV - ${SITE_TITLE}`} description="Professional CV and experience of Anton Orlov - Entrepreneur & Student" />
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link 
      rel="preload" 
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
      as="style" 
      onload="this.onload=null;this.rel='stylesheet'"
    />
    <noscript>
      <link 
        href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" 
        rel="stylesheet"
      />
    </noscript>
  </head>
  <body>
    <Header />

    <!-- CV Hero Section -->
    <section class="cv-hero">
      <div class="cv-hero-container">
        <div class="cv-profile-section">
          <div class="profile-image-wrapper">
            <img src="/about-photo.jpg" alt="Anton Orlov" class="profile-image" />
            <div class="profile-image-overlay"></div>
          </div>
          <div class="profile-info">
            <h1 class="profile-name">Anton Orlov</h1>
            <p class="profile-title">Entrepreneur & Student</p>
            <p class="profile-institution">Webster University Vienna</p>
            <div class="profile-summary">
              Enthusiastic BA candidate with entrepreneurial experience, proven ability to design processes, lead teams, and implement AI-driven solutions for forward-thinking companies.
            </div>
            <div class="profile-actions">
              <a href="mailto:orlovantonle@gmail.com" class="btn-primary">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                  <polyline points="22,6 12,13 2,6"/>
                </svg>
                Contact Me
              </a>
              <a href="https://www.linkedin.com/in/anton-orlov-b16126269/" class="btn-secondary" target="_blank">
                <svg viewBox="0 0 24 24" fill="currentColor">
                  <path d="M19 0h-14c-2.76 0-5 2.24-5 5v14c0 2.76 2.24 5 5 5h14c2.76 0 5-2.24 5-5v-14c0-2.76-2.24-5-5-5zm-11.5 20h-3v-9h3v9zm-1.5-10.25c-.966 0-1.75-.784-1.75-1.75S5.534 6.5 6.5 6.5s1.75.784 1.75 1.75-.784 1.75-1.75 1.75zm13 10.25h-3v-4.5c0-1.08-.02-2.47-1.5-2.47-1.5 0-1.73 1.17-1.73 2.38v4.59h-3v-9h2.89v1.23h.04c.403-.76 1.387-1.56 2.855-1.56 3.053 0 3.62 2.008 3.62 4.62v4.71z"/>
                </svg>
                LinkedIn
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CV Content Section -->
    <section class="cv-content">
      <div class="container">
        <div class="cv-layout">
          <!-- Main Content -->
          <div class="cv-main">
            <!-- Experience Section -->
            <section class="cv-section">
              <div class="section-header">
                <h2 class="section-title">
                  <span class="section-icon">💼</span>
                  Entrepreneurial Experience
                </h2>
              </div>
              
              <div class="experience-timeline">
                <div class="experience-item">
                  <div class="experience-timeline-dot"></div>
                  <div class="experience-content">
                    <div class="experience-header">
                      <h3>Co-Founder & CEO</h3>
                      <div class="experience-meta">
                        <span class="company">BSC, Vienna, Austria</span>
                        <span class="period">Jan 2025 – Present</span>
                      </div>
                    </div>
                    <div class="experience-description">
                      <p>Leading the development of innovative business-learning events and mentorship programs connecting students with top entrepreneurs.</p>
                      <ul class="achievement-list">
                        <li>Launched interactive business events for 100+ students with 95% satisfaction rate</li>
                        <li>Developed 4-5 week mentorship course with AI-powered analytics and practical business building</li>
                        <li>Led cross-functional team of 5, reducing planning time by 30% through streamlined processes</li>
                        <li>Organized end-to-end event operations including vendor sourcing, scheduling, and marketing</li>
                      </ul>
                    </div>
                  </div>
                </div>

                <div class="experience-item">
                  <div class="experience-timeline-dot"></div>
                  <div class="experience-content">
                    <div class="experience-header">
                      <h3>Founder & Managing Director</h3>
                      <div class="experience-meta">
                        <span class="company">Olive Crown Estate, Girne Cyprus</span>
                        <span class="period">Mar 2022 – Mar 2023</span>
                      </div>
                    </div>
                    <div class="experience-description">
                      <p>Established and operated a boutique real estate agency specializing in residential property sales and rentals.</p>
                      <ul class="achievement-list">
                        <li>Built successful property sales and rental business from ground up</li>
                        <li>Implemented CRM and automated workflow systems, cutting administrative overhead by 40%</li>
                        <li>Negotiated contracts and managed complex client relationships</li>
                        <li>Coordinated remote and local teams, fostering collaborative culture</li>
                      </ul>
                    </div>
                  </div>
                </div>
              </div>
            </section>

            <!-- Interests Section -->
            <section class="cv-section">
              <div class="section-header">
                <h2 class="section-title">
                  <span class="section-icon">🎯</span>
                  Interests & Activities
                </h2>
              </div>
              
              <div class="interests-grid">
                <div class="interest-card">
                  <div class="interest-icon">🏋️‍♂️</div>
                  <h3>Fitness & Sports</h3>
                  <p>Active gym-goer, climbing enthusiast, and participant in combat sports</p>
                </div>
                <div class="interest-card">
                  <div class="interest-icon">🤖</div>
                  <h3>AI Community</h3>
                  <p>Attendee and volunteer at local Vienna AI meetups and technology events</p>
                </div>
              </div>
            </section>
          </div>

          <!-- Sidebar -->
          <div class="cv-sidebar">
            <!-- Education Section -->
            <section class="cv-section">
              <div class="section-header">
                <h2 class="section-title">
                  <span class="section-icon">🎓</span>
                  Education
                </h2>
              </div>
              
              <div class="education-list">
                <div class="education-item">
                  <div class="education-content">
                    <h3>Bachelor of Business Administration</h3>
                    <div class="education-meta">
                      <span class="institution">Webster University Vienna</span>
                      <span class="period">Expected May 2026</span>
                    </div>
                    <div class="gpa-badge">GPA: 3.5/4.0</div>
                    <div class="coursework">
                      <h4>Key Coursework:</h4>
                      <div class="course-tags">
                        <span class="course-tag">Financial Analysis</span>
                        <span class="course-tag">Business Process Management</span>
                        <span class="course-tag">Data Analytics</span>
                        <span class="course-tag">AI in Business</span>
                      </div>
                    </div>
                  </div>
                </div>

                <div class="education-item">
                  <div class="education-content">
                    <h3>Finance Studies</h3>
                    <div class="education-meta">
                      <span class="institution">Financial University, Moscow</span>
                      <span class="period">2019 – 2022</span>
                    </div>
                    <p class="education-description">
                      Completed three years toward Bachelor in Finance with foundational expertise in corporate finance, risk management, and financial modeling.
                    </p>
                  </div>
                </div>
              </div>
            </section>

            <!-- Skills Section -->
            <section class="cv-section">
              <div class="section-header">
                <h2 class="section-title">
                  <span class="section-icon">⚡</span>
                  Key Skills
                </h2>
              </div>
              
              <div class="skills-list">
                <div class="skill-item">
                  <div class="skill-icon">🔧</div>
                  <div class="skill-content">
                    <h3>Process Optimization</h3>
                    <p>Mapping, automation and continuous improvement of business workflows</p>
                  </div>
                </div>
                <div class="skill-item">
                  <div class="skill-icon">🤖</div>
                  <div class="skill-content">
                    <h3>AI & Data Literacy</h3>
                    <p>Applying machine learning tools for predictive insights and operations</p>
                  </div>
                </div>
                <div class="skill-item">
                  <div class="skill-icon">📋</div>
                  <div class="skill-content">
                    <h3>Project Management</h3>
                    <p>End-to-end planning, budgeting and execution of complex programs</p>
                  </div>
                </div>
                <div class="skill-item">
                  <div class="skill-icon">👥</div>
                  <div class="skill-content">
                    <h3>Leadership</h3>
                    <p>Guiding teams, setting objectives and nurturing professional growth</p>
                  </div>
                </div>
              </div>
            </section>

            <!-- Languages Section -->
            <section class="cv-section">
              <div class="section-header">
                <h2 class="section-title">
                  <span class="section-icon">🌍</span>
                  Languages
                </h2>
              </div>
              
              <div class="languages-list">
                <div class="language-item">
                  <span class="language-name">Russian</span>
                  <span class="language-level native">Native</span>
                </div>
                <div class="language-item">
                  <span class="language-name">English</span>
                  <span class="language-level fluent">Fluent</span>
                </div>
                <div class="language-item">
                  <span class="language-name">Spanish</span>
                  <span class="language-level basic">Basic</span>
                </div>
                <div class="language-item">
                  <span class="language-name">German</span>
                  <span class="language-level basic">Basic</span>
                </div>
              </div>
            </section>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cv-cta">
      <div class="container">
        <div class="cta-content">
          <h2>Why I'm a Perfect Fit</h2>
          <p>
            As both a student and entrepreneur, I thrive on learning and adapting. My hands-on ventures demonstrate a pragmatic approach to optimizing resources and implementing AI to drive efficiency. I'm excited to contribute a fresh, analytical mindset and genuine humility to a European team seeking sustainable, cost-effective innovations.
          </p>
          <div class="cta-actions">
            <a href="mailto:orlovantonle@gmail.com" class="btn-primary">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
              Email Me
            </a>
            <a href="https://www.linkedin.com/in/anton-orlov-b16126269/" class="btn-secondary" target="_blank">
              <svg viewBox="0 0 24 24" fill="currentColor">
                <path d="M19 0h-14c-2.76 0-5 2.24-5 5v14c0 2.76 2.24 5 5 5h14c2.76 0 5-2.24 5-5v-14c0-2.76-2.24-5-5-5zm-11.5 20h-3v-9h3v9zm-1.5-10.25c-.966 0-1.75-.784-1.75-1.75S5.534 6.5 6.5 6.5s1.75.784 1.75 1.75-.784 1.75-1.75 1.75zm13 10.25h-3v-4.5c0-1.08-.02-2.47-1.5-2.47-1.5 0-1.73 1.17-1.73 2.38v4.59h-3v-9h2.89v1.23h.04c.403-.76 1.387-1.56 2.855-1.56 3.053 0 3.62 2.008 3.62 4.62v4.71z"/>
              </svg>
              Connect on LinkedIn
            </a>
          </div>
        </div>
      </div>
    </section>

    <Footer />

    <script>
      // Performance-optimized intersection observer for CV animations
      const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -30px 0px'
      };

      // Scroll performance optimization
      let isScrolling = false;
      let scrollTimeout: number | null = null;
      
      const handleScrollStart = () => {
        if (!isScrolling) {
          isScrolling = true;
          document.body.classList.add('is-scrolling');
        }
        
        if (scrollTimeout) {
          clearTimeout(scrollTimeout);
        }
        
        scrollTimeout = setTimeout(() => {
          isScrolling = false;
          document.body.classList.remove('is-scrolling');
          scrollTimeout = null;
        }, 150) as unknown as number;
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            requestAnimationFrame(() => {
              entry.target.classList.add('animate-in');
            });
            observer.unobserve(entry.target);
          }
        });
      }, observerOptions);

      document.addEventListener('DOMContentLoaded', () => {
        // Observe CV sections for scroll animations
        const elementsToObserve = document.querySelectorAll(
          '.cv-section, .experience-item, .interest-card, .education-item, .skill-item'
        );
        
        elementsToObserve.forEach(el => {
          observer.observe(el);
        });

        // Timeline animation stagger
        const experienceItems = document.querySelectorAll('.experience-item');
        experienceItems.forEach((item, index) => {
          (item as HTMLElement).style.animationDelay = `${index * 0.2}s`;
        });

        // Optimized scroll listener
        window.addEventListener('scroll', handleScrollStart, { passive: true });

        // Intersection Observer for performance optimization
        const performanceObserver = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            const element = entry.target as HTMLElement;
            if (entry.isIntersecting) {
              element.classList.add('in-viewport');
            } else {
              element.classList.remove('in-viewport');
            }
          });
        }, { threshold: 0 });

        // Observe all interactive elements for viewport-based optimizations
        const interactiveElements = document.querySelectorAll(
          '.experience-content, .interest-card, .education-item, .skill-item, .language-item'
        );
        
        interactiveElements.forEach(el => {
          performanceObserver.observe(el);
        });

        // Clean up GPU acceleration after animations
        setTimeout(() => {
          elementsToObserve.forEach(el => {
            (el as HTMLElement).style.willChange = 'auto';
          });
        }, 3000);
      });

      // Cleanup on page unload
      window.addEventListener('beforeunload', () => {
        observer.disconnect();
        if (scrollTimeout) {
          clearTimeout(scrollTimeout);
        }
      });
    </script>
  </body>
</html>

<style>
  /* Performance-optimized CV styles using design system from index page */
  
  /* CSS Variables - Consistent with main site */
  :root {
    --primary-bg: #0a0a0a;
    --secondary-bg: #111111;
    --surface: #1a1a1a;
    --surface-elevated: #222222;
    --primary-text: #ffffff;
    --secondary-text: #b8b8b8;
    --accent-text: #f0f0f0;
    --accent-orange: #d38f41;
    --accent-purple: #64155f;
    --primary-gradient: linear-gradient(135deg, #d38f41 0%, #ff6600 50%, #64155f 100%);
    --border: rgba(255, 255, 255, 0.12);
    --border-hover: rgba(211, 143, 65, 0.3);
    --shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.6);
    --shadow-colored: 0 25px 50px -12px rgba(211, 143, 65, 0.25);
    --font-primary: 'Inter', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
    --transition-fast: 0.15s cubic-bezier(0.4, 0, 0.2, 1);
    --transition-normal: 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    --transition-slow: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  body {
    font-family: var(--font-primary);
    background-color: var(--primary-bg);
    color: var(--primary-text);
    line-height: 1.6;
    overflow-x: hidden;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  /* Disable expensive hover effects during scroll */
  .is-scrolling .experience-content:hover,
  .is-scrolling .interest-card:hover,
  .is-scrolling .education-item:hover,
  .is-scrolling .skill-item:hover,
  .is-scrolling .language-item:hover {
    transform: none !important;
    box-shadow: none !important;
    border-color: var(--border) !important;
    background: var(--surface-elevated) !important;
  }

  /* Reduce will-change usage - only apply when needed */
  .in-viewport.experience-content,
  .in-viewport.interest-card,
  .in-viewport.education-item,
  .in-viewport.skill-item,
  .in-viewport.language-item {
    will-change: transform;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* CV Hero Section */
  .cv-hero {
    min-height: 70vh;
    display: flex;
    align-items: center;
    background: radial-gradient(ellipse at top, rgba(211, 143, 65, 0.15) 0%, transparent 50%),
                radial-gradient(ellipse at bottom, rgba(100, 21, 95, 0.15) 0%, transparent 50%),
                var(--primary-bg);
    position: relative;
    padding: 2rem 0;
    transform: translateZ(0);
  }

  .cv-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(600px circle at 30% 20%, rgba(211, 143, 65, 0.1), transparent 40%),
      radial-gradient(800px circle at 70% 80%, rgba(100, 21, 95, 0.1), transparent 40%);
    pointer-events: none;
    transform: translateZ(0);
  }

  .cv-hero-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 1;
  }

  .cv-profile-section {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 3rem;
    align-items: center;
    animation: fadeInUp 0.6s ease-out;
  }

  .profile-image-wrapper {
    position: relative;
    width: 200px;
    height: 200px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid transparent;
    background: var(--primary-gradient);
    padding: 3px;
    transform: translateZ(0);
  }

  .profile-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
    transition: transform var(--transition-fast);
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  .profile-image-wrapper:hover .profile-image {
    transform: scale(1.03) translateZ(0);
  }

  .profile-image-overlay {
    position: absolute;
    inset: 3px;
    background: var(--primary-gradient);
    opacity: 0.1;
    border-radius: 50%;
    transition: opacity var(--transition-fast);
    pointer-events: none;
    transform: translateZ(0);
  }

  .profile-image-wrapper:hover .profile-image-overlay {
    opacity: 0.15;
  }

  .profile-name {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 800;
    margin-bottom: 0.5rem;
    background: var(--primary-gradient);
    background-size: 200% 200%;
    animation: gradientShift 4s ease infinite;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: -0.02em;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  .profile-title {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--accent-text);
    margin-bottom: 0.5rem;
  }

  .profile-institution {
    font-size: 1.1rem;
    color: var(--secondary-text);
    margin-bottom: 1.5rem;
  }

  .profile-summary {
    font-size: 1.1rem;
    color: var(--secondary-text);
    line-height: 1.7;
    margin-bottom: 2rem;
    max-width: 600px;
  }

  .profile-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Button Styles - Optimized */
  .btn-primary,
  .btn-secondary {
    padding: 0.875rem 1.75rem;
    border-radius: 0.75rem;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
    transition: transform var(--transition-fast), box-shadow var(--transition-fast);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    overflow: hidden;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  .btn-primary {
    background: var(--primary-gradient);
    background-size: 200% 200%;
    animation: gradientShift 3s ease infinite;
    color: white;
    border: none;
    box-shadow: var(--shadow-colored);
  }

  .btn-primary:hover {
    transform: translateY(-1px) translateZ(0);
    box-shadow: 0 10px 20px rgba(211, 143, 65, 0.3);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.05);
    color: var(--primary-text);
    border: 2px solid var(--border);
    backdrop-filter: blur(10px);
  }

  .btn-secondary:hover {
    background: rgba(211, 143, 65, 0.08);
    border-color: var(--accent-orange);
    transform: translateY(-1px) translateZ(0);
    color: var(--accent-orange);
  }

  .btn-primary svg,
  .btn-secondary svg {
    width: 1rem;
    height: 1rem;
  }

  /* CV Content Section */
  .cv-content {
    padding: 6rem 0;
    background: linear-gradient(180deg, var(--secondary-bg) 0%, var(--primary-bg) 100%);
    position: relative;
    transform: translateZ(0);
  }

  .cv-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(600px circle at 80% 20%, rgba(100, 21, 95, 0.08), transparent 40%),
      radial-gradient(800px circle at 20% 80%, rgba(211, 143, 65, 0.08), transparent 40%);
    pointer-events: none;
    transform: translateZ(0);
  }

  .cv-layout {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 4rem;
    position: relative;
    z-index: 1;
  }

  /* Section Styles */
  .cv-section {
    margin-bottom: 3rem;
    opacity: 0;
    transform: translateY(30px) translateZ(0);
    transition: all 0.6s ease;
  }

  .cv-section.animate-in {
    opacity: 1;
    transform: translateY(0) translateZ(0);
  }

  .section-header {
    margin-bottom: 2rem;
  }

  .section-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--accent-text);
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .section-icon {
    font-size: 1.5rem;
    width: 2.5rem;
    height: 2.5rem;
    background: var(--primary-gradient);
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transform: translateZ(0);
  }

  /* Experience Timeline */
  .experience-timeline {
    position: relative;
  }

  .experience-timeline::before {
    content: '';
    position: absolute;
    left: 1.25rem;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--border);
  }

  .experience-item {
    position: relative;
    margin-bottom: 3rem;
    opacity: 0;
    transform: translateX(-20px) translateZ(0);
    animation: slideInLeft 0.6s ease forwards;
  }

  .experience-item.animate-in {
    opacity: 1;
    transform: translateX(0) translateZ(0);
  }

  .experience-timeline-dot {
    position: absolute;
    left: 0.75rem;
    top: 0.5rem;
    width: 1rem;
    height: 1rem;
    background: var(--primary-gradient);
    border-radius: 50%;
    transform: translateZ(0);
  }

  /* Optimized experience content hover */
  .experience-content {
    margin-left: 3.5rem;
    padding: 1.5rem;
    background: var(--surface-elevated);
    border-radius: 1rem;
    border: 1px solid var(--border);
    transition: transform var(--transition-fast), border-color var(--transition-fast);
    backdrop-filter: blur(10px);
    transform: translateZ(0);
  }

  .experience-content:hover {
    transform: translateY(-2px) translateZ(0);
    border-color: var(--accent-orange);
  }

  .experience-header h3 {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--accent-text);
    margin-bottom: 0.5rem;
  }

  .experience-meta {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    margin-bottom: 1rem;
  }

  .company {
    font-weight: 500;
    color: var(--primary-text);
  }

  .period {
    font-size: 0.9rem;
    color: var(--secondary-text);
    font-family: var(--font-mono);
  }

  .experience-description p {
    color: var(--secondary-text);
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  .achievement-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .achievement-list li {
    position: relative;
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    color: var(--secondary-text);
    line-height: 1.6;
    transition: transform var(--transition-fast);
  }

  .achievement-list li:hover {
    transform: translateX(3px) translateZ(0);
  }

  .achievement-list li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: var(--accent-orange);
    font-weight: bold;
  }

  /* Interests Grid - Optimized */
  .interests-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .interest-card {
    padding: 2rem 1.5rem;
    background: var(--surface-elevated);
    border-radius: 1rem;
    border: 1px solid var(--border);
    text-align: center;
    transition: transform var(--transition-fast), border-color var(--transition-fast);
    backdrop-filter: blur(10px);
    transform: translateZ(0);
  }

  .interest-card:hover {
    transform: translateY(-3px) translateZ(0);
    border-color: var(--accent-orange);
  }

  .interest-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    display: block;
  }

  .interest-card h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--accent-text);
    margin-bottom: 0.75rem;
  }

  .interest-card p {
    color: var(--secondary-text);
    font-size: 0.95rem;
    line-height: 1.5;
  }

  /* Education List - Optimized */
  .education-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .education-item {
    padding: 1.5rem;
    background: var(--surface-elevated);
    border-radius: 1rem;
    border: 1px solid var(--border);
    transition: transform var(--transition-fast), border-color var(--transition-fast);
    backdrop-filter: blur(10px);
    transform: translateZ(0);
  }

  .education-item:hover {
    transform: translateY(-2px) translateZ(0);
    border-color: var(--accent-orange);
  }

  .education-content h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--accent-text);
    margin-bottom: 0.75rem;
  }

  .education-meta {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    margin-bottom: 1rem;
  }

  .institution {
    font-weight: 500;
    color: var(--primary-text);
    font-size: 0.95rem;
  }

  .gpa-badge {
    display: inline-block;
    padding: 0.375rem 0.75rem;
    background: rgba(211, 143, 65, 0.15);
    border: 1px solid var(--accent-orange);
    border-radius: 1rem;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--accent-orange);
    margin-bottom: 1rem;
    font-family: var(--font-mono);
  }

  .coursework h4 {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--accent-text);
    margin-bottom: 0.75rem;
  }

  .course-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .course-tag {
    padding: 0.25rem 0.75rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--border);
    border-radius: 1rem;
    font-size: 0.8rem;
    color: var(--secondary-text);
    transition: all var(--transition-fast);
  }

  .course-tag:hover {
    background: rgba(211, 143, 65, 0.08);
    border-color: var(--accent-orange);
    color: var(--accent-orange);
  }

  .education-description {
    color: var(--secondary-text);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  /* Skills List - Optimized */
  .skills-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .skill-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    padding: 1.25rem;
    background: var(--surface-elevated);
    border-radius: 0.75rem;
    border: 1px solid var(--border);
    transition: transform var(--transition-fast), border-color var(--transition-fast);
    backdrop-filter: blur(10px);
    transform: translateZ(0);
  }

  .skill-item:hover {
    transform: translateX(3px) translateZ(0);
    border-color: var(--border-hover);
  }

  .skill-icon {
    font-size: 1.5rem;
    width: 2.5rem;
    height: 2.5rem;
    background: var(--primary-gradient);
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transform: translateZ(0);
  }

  .skill-content h3 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--accent-text);
    margin-bottom: 0.5rem;
  }

  .skill-content p {
    color: var(--secondary-text);
    font-size: 0.9rem;
    line-height: 1.5;
  }

  /* Languages List - Optimized */
  .languages-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .language-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.875rem 1.25rem;
    background: var(--surface-elevated);
    border-radius: 0.75rem;
    border: 1px solid var(--border);
    transition: transform var(--transition-fast), border-color var(--transition-fast);
    backdrop-filter: blur(10px);
    transform: translateZ(0);
  }

  .language-item:hover {
    transform: translateY(-1px) translateZ(0);
    border-color: var(--accent-orange);
  }

  .language-name {
    font-weight: 500;
    color: var(--primary-text);
  }

  .language-level {
    font-size: 0.85rem;
    padding: 0.25rem 0.75rem;
    border-radius: 1rem;
    font-weight: 500;
    font-family: var(--font-mono);
  }

  .language-level.native {
    background: rgba(34, 197, 94, 0.15);
    color: #22c55e;
    border: 1px solid rgba(34, 197, 94, 0.3);
  }

  .language-level.fluent {
    background: rgba(59, 130, 246, 0.15);
    color: #3b82f6;
    border: 1px solid rgba(59, 130, 246, 0.3);
  }

  .language-level.basic {
    background: rgba(251, 191, 36, 0.15);
    color: #fbbf24;
    border: 1px solid rgba(251, 191, 36, 0.3);
  }

  /* CV CTA Section */
  .cv-cta {
    padding: 5rem 0;
    background: var(--primary-bg);
    position: relative;
    text-align: center;
    transform: translateZ(0);
  }

  .cv-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(600px circle at 50% 50%, rgba(211, 143, 65, 0.1), transparent 40%);
    pointer-events: none;
    transform: translateZ(0);
  }

  .cta-content {
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .cta-content h2 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    background: var(--primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    transform: translateZ(0);
  }

  .cta-content p {
    font-size: 1.1rem;
    color: var(--secondary-text);
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  .cta-actions {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px) translateZ(0);
    }
    to {
      opacity: 1;
      transform: translateY(0) translateZ(0);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px) translateZ(0);
    }
    to {
      opacity: 1;
      transform: translateX(0) translateZ(0);
    }
  }

  @keyframes gradientShift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .cv-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .cv-profile-section {
      gap: 2rem;
    }

    /* Simplify hover effects on tablets */
    .experience-content:hover,
    .interest-card:hover,
    .education-item:hover,
    .skill-item:hover,
    .language-item:hover {
      transform: none;
    }
  }

  @media (max-width: 768px) {
    .cv-profile-section {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }

    .profile-image-wrapper {
      width: 150px;
      height: 150px;
      margin: 0 auto;
    }

    .profile-actions {
      justify-content: center;
    }

    .experience-timeline::before {
      display: none;
    }

    .experience-content {
      margin-left: 0;
    }

    .experience-timeline-dot {
      display: none;
    }

    .interests-grid {
      grid-template-columns: 1fr;
    }

    .cta-actions {
      flex-direction: column;
      align-items: center;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      max-width: 300px;
      justify-content: center;
    }

    .container {
      padding: 0 1rem;
    }

    /* Disable hover effects on mobile */
    .experience-content:hover,
    .interest-card:hover,
    .education-item:hover,
    .skill-item:hover,
    .language-item:hover {
      transform: none;
      border-color: var(--border);
    }
  }

  @media (max-width: 480px) {
    .cv-hero {
      min-height: 60vh;
      padding: 1rem 0;
    }

    .cv-content {
      padding: 3rem 0;
    }

    .cv-cta {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 1.5rem;
    }

    .profile-name {
      font-size: clamp(2rem, 8vw, 2.5rem);
    }

    /* Disable complex animations on small screens */
    .btn-primary {
      animation: none;
    }

    .profile-name {
      animation: none;
      background: var(--primary-gradient);
    }

    /* Disable all hover effects on very small screens */
    * {
      transition: none !important;
    }
  }

  /* Accessibility & Performance */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.15s !important;
    }

    .profile-name,
    .btn-primary {
      animation: none !important;
    }

    .is-scrolling * {
      transition: none !important;
    }
  }

  @media (prefers-reduced-data: reduce) {
    .cv-hero::before,
    .cv-content::before,
    .cv-cta::before {
      display: none;
    }

    * {
      animation: none !important;
      transition-duration: 0.1s !important;
    }
  }

  /* High contrast support */
  @media (prefers-contrast: high) {
    :root {
      --border: rgba(255, 255, 255, 0.3);
      --secondary-text: #cccccc;
    }
  }

  /* GPU acceleration for performance - only essential elements */
  .profile-image,
  .btn-primary,
  .btn-secondary {
    transform: translateZ(0);
    backface-visibility: hidden;
  }
</style>
