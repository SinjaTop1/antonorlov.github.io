---
// Import your React component
import BSCCarousel from "../components/BSCCarousel";
---

<section class="next-section">
  <div class="particles-container" id="particles-js"></div>

  <div class="section-heading reveal-element">
    <h1>BSC</h1>
    <div class="subtitle-wrapper">
      <h2>My Current Project</h2>
    </div>
  </div>

  <div class="BSC-left reveal-element">
    <div class="content-wrapper glass-effect">
      <h2 class="gradient-text">Connecting Ambitious Minds</h2>

      <p class="mission-statement">
        We bridge the gap between entrepreneurial-minded students and industry leaders, creating opportunities that transform ideas into reality.
      </p>

      <div class="key-points">
        <div class="key-point hover-effect">
          <div class="icon-wrapper">
            <span class="icon">🔗</span>
          </div>
          <p>Connecting students with leading professionals through networking events and Masterclasses.</p>
        </div>

        <div class="key-point hover-effect">
          <div class="icon-wrapper">
            <span class="icon">💡</span>
          </div>
          <p>Designing practical workshops and seminars that provide real-world business knowledge and entrepreneurial skills.</p>
        </div>

        <div class="key-point hover-effect">
          <div class="icon-wrapper">
            <span class="icon">🚀</span>
          </div>
          <p>Making business accessible by providing students with the tools, resources, and community needed to launch successful ventures.</p>
        </div>
      </div>

      <div class="cta-container">
        <a
          href="https://www.bsc-at.com/"
          class="cta-button"
          target="_blank"
          rel="noreferrer"
        >
          <span>Visit Us</span>
          <svg class="arrow-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <div class="BSC-right reveal-element">
    <div class="carousel-wrapper glass-effect">
      <BSCCarousel client:only="react" />
    </div>
  </div>
</section>

<script>
  // Optimized reveal animation script with Intersection Observer
  document.addEventListener("DOMContentLoaded", () => {
    const revealElements = document.querySelectorAll(".reveal-element");
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("revealed");
          observer.unobserve(entry.target);
        }
      });
    }, {
      threshold: 0.1,
      rootMargin: "0px 0px -50px 0px"
    });

    revealElements.forEach(element => {
      observer.observe(element);
    });
  });
</script>

<style>
.next-section {
  position: relative;
  display: flex;
  flex-wrap: wrap;
  padding: 6rem 2rem;
  min-height: 100vh;
  background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
  overflow: hidden;
}

.particles-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

.glass-effect {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
}

.section-heading {
  width: 100%;
  margin-bottom: 4rem;
  text-align: center;
  position: relative;
  z-index: 1;
}

.section-heading h1 {
  font-size: 4rem;
  font-weight: 800;
  background: linear-gradient(45deg, #d38f41, #ff6600, #64155f);
  background-size: 200% 200%;
  animation: gradientFlow 8s ease infinite;
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  margin-bottom: 1rem;
}

.subtitle-wrapper {
  position: relative;
  display: inline-block;
}

.section-heading h2 {
  font-size: 1.2rem;
  color: #ffffff;
  text-transform: uppercase;
  letter-spacing: 0.3em;
  opacity: 0.8;
}

.BSC-left {
  flex: 1;
  padding: 2rem;
  min-width: 300px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  position: relative;
  z-index: 1;
}

.content-wrapper {
  max-width: 600px;
  margin: 0 auto;
  padding: 3rem;
  border-radius: 20px;
  transition: transform 0.3s ease;
}

.content-wrapper:hover {
  transform: translateY(-5px);
}

.gradient-text {
  font-size: 2.2rem;
  background: linear-gradient(45deg, #ffffff, #d38f41);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  text-align: center;
  margin-bottom: 2rem;
}

.mission-statement {
  text-align: center;
  font-size: 1.3rem;
  line-height: 1.8;
  color: rgba(255, 255, 255, 0.9);
  margin-bottom: 3rem;
  font-weight: 400;
}

.key-points {
  display: grid;
  gap: 2rem;
  margin-bottom: 3rem;
}

.key-point {
  display: flex;
  align-items: flex-start;
  gap: 1.5rem;
  padding: 1.5rem;
  border-radius: 12px;
  transition: all 0.3s ease;
}

.key-point:hover {
  background: rgba(255, 255, 255, 0.05);
  transform: translateX(10px);
}

.icon-wrapper {
  background: linear-gradient(135deg, rgba(211, 143, 65, 0.2), rgba(100, 21, 95, 0.2));
  padding: 1rem;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon {
  font-size: 1.5rem;
}

.key-point p {
  color: rgba(255, 255, 255, 0.9);
  font-size: 1.1rem;
  line-height: 1.6;
  margin: 0;
}

.cta-container {
  text-align: center;
  margin-top: 2rem;
}

.cta-button {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  background: linear-gradient(45deg, #d38f41, #ff6600);
  color: white;
  text-decoration: none;
  border-radius: 30px;
  font-weight: 600;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.cta-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(211, 143, 65, 0.3);
}

.arrow-icon {
  width: 20px;
  height: 20px;
  transition: transform 0.3s ease;
}

.cta-button:hover .arrow-icon {
  transform: translateX(5px);
}

.BSC-right {
  flex: 1;
  padding: 2rem;
  min-width: 300px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 1;
}

.carousel-wrapper {
  width: 100%;
  max-width: 800px;
  aspect-ratio: 16/9;
  border-radius: 20px;
  overflow: hidden;
  margin: 0 auto;
}

:global(.embla) {
  width: 100%;
  height: 100%;
}

:global(.embla__viewport) {
  width: 100%;
  height: 100%;
}

:global(.embla__container) {
  width: 100%;
  height: 100%;
  display: flex;
}

:global(.embla__slide) {
  flex: 0 0 100%;
  min-width: 0;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

:global(.embla__slide img) {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center;
}

:global(.embla__button) {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background-color: rgba(255, 255, 255, 0.8);
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

:global(.embla__button:hover) {
  background-color: rgba(255, 255, 255, 0.95);
  transform: scale(1.1);
}

:global(.embla__button--prev) {
  left: 10px;
}

:global(.embla__button--next) {
  right: 10px;
}

.reveal-element {
  opacity: 0;
  transform: translateY(30px);
  transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}

.reveal-element.revealed {
  opacity: 1;
  transform: translateY(0);
}

@keyframes gradientFlow {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* Responsive Styles */
@media (max-width: 1024px) {
  .next-section {
    padding: 5rem 1.5rem;
  }
  
  .section-heading h1 {
    font-size: 3.5rem;
  }
  
  .content-wrapper {
    padding: 2.5rem;
  }
  
  .gradient-text {
    font-size: 2rem;
  }
  
  .mission-statement {
    font-size: 1.2rem;
  }
  
  .key-point p {
    font-size: 1rem;
  }
}

@media (max-width: 768px) {
  .next-section {
    padding: 4rem 1rem;
    flex-direction: column;
  }
  
  .section-heading {
    margin-bottom: 3rem;
  }
  
  .section-heading h1 {
    font-size: 3rem;
    letter-spacing: 0.15em;
  }
  
  .section-heading h2 {
    font-size: 1rem;
    letter-spacing: 0.2em;
  }
  
  .BSC-left, .BSC-right {
    padding: 1rem;
    width: 100%;
  }
  
  .content-wrapper {
    padding: 2rem;
  }
  
  .gradient-text {
    font-size: 1.8rem;
  }
  
  .mission-statement {
    font-size: 1.1rem;
    margin-bottom: 2rem;
  }
  
  .key-points {
    gap: 1.5rem;
    margin-bottom: 2rem;
  }
  
  .key-point {
    padding: 1.2rem;
  }
  
  .icon-wrapper {
    padding: 0.8rem;
  }
  
  .icon {
    font-size: 1.3rem;
  }
  
  .key-point p {
    font-size: 0.95rem;
  }
  
  .carousel-wrapper {
    margin-top: 2rem;
  }
}

@media (max-width: 480px) {
  .next-section {
    padding: 3rem 0.75rem;
  }
  
  .section-heading h1 {
    font-size: 2.5rem;
    letter-spacing: 0.1em;
  }
  
  .section-heading h2 {
    font-size: 0.9rem;
    letter-spacing: 0.15em;
  }
  
  .content-wrapper {
    padding: 1.5rem;
  }
  
  .gradient-text {
    font-size: 1.5rem;
  }
  
  .mission-statement {
    font-size: 1rem;
    line-height: 1.6;
  }
  
  .key-points {
    gap: 1rem;
  }
  
  .key-point {
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1rem;
  }
  
  .icon-wrapper {
    margin-bottom: 0.8rem;
  }
  
  .cta-button {
    padding: 0.8rem 1.5rem;
    font-size: 0.9rem;
  }
  
  .arrow-icon {
    width: 16px;
    height: 16px;
  }
  
  :global(.embla__button) {
    width: 32px;
    height: 32px;
  }
}
</style>