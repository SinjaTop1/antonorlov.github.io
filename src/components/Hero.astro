---
---

<section class="hero" id="home">
  <div class="hero-left">
    <h1 class="name">
      Hello,
    </h1>
    <h2 class="tagline">
      I am <span class="name-spacing"></span><span class="highlight">Anton Orlov</span> — Entrepreneur and Student,
    </h2>
    <div class="summary-wrapper">
      <div class="summary-container">
        <p class="summary">
           I have experience working on multiple projects and am currently leading one.
           <br><br>
           I've traveled the world and had the privilege of building successful teams from the ground up.
           <br><br>
           My passion lies in creation and innovation, and my dream is to empower others to do the same.
        </p>
      </div>
    </div>
    
    <!-- Link circles with official LinkedIn and Instagram logos -->
    <div class="link-circles">
      <a href="https://www.linkedin.com/in/anton-orlov-b16126269/" class="circle-link" aria-label="LinkedIn" target="_blank">
        <svg role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
          <title>LinkedIn</title>
          <path fill="#ffffff" d="M19 0h-14c-2.76 0-5 2.24-5 5v14c0 2.76 2.24 5 5 5h14c2.76 0 5-2.24 5-5v-14c0-2.76-2.24-5-5-5zm-11.5 20h-3v-9h3v9zm-1.5-10.25c-.966 0-1.75-.784-1.75-1.75S5.534 6.5 6.5 6.5s1.75.784 1.75 1.75-.784 1.75-1.75 1.75zm13 10.25h-3v-4.5c0-1.08-.02-2.47-1.5-2.47-1.5 0-1.73 1.17-1.73 2.38v4.59h-3v-9h2.89v1.23h.04c.403-.76 1.387-1.56 2.855-1.56 3.053 0 3.62 2.008 3.62 4.62v4.71z"/>
        </svg>
      </a>
      <a href="https://www.instagram.com/orochiyo?igsh=MXd4MW9sMHN6czI2eQ==" class="circle-link" aria-label="Instagram" target="_blank">
        <svg role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
          <title>Instagram</title>
          <path fill="#ffffff" d="M12 2.2c3.1 0 3.5 0 4.7.1 1.2.1 1.9.3 2.3.5.6.2 1 .5 1.5 1 .5.5.8.9 1 1.5.2.4.4 1.1.5 2.3.1 1.2.1 1.6.1 4.7s0 3.5-.1 4.7c-.1 1.2-.3 1.9-.5 2.3-.2.6-.5 1-.9 1.5-.5.5-.9.8-1.5 1-.4.2-1.1.4-2.3.5-1.2.1-1.6.1-4.7.1s-3.5 0-4.7-.1c-1.2-.1-1.9-.3-2.3-.5-.6-.2-1-.5-1.5-1-.5-.5-.8-.9-1-1.5-.2-.4-.4-1.1-.5-2.3-.1-1.2-.1-1.6-.1-4.7s0-3.5.1-4.7c.1-1.2.3-1.9.5-2.3.2-.6.5-1 1-1.5.5-.5.9-.8 1.5-1 .4-.2 1.1-.4 2.3-.5 1.2-.1 1.6-.1 4.7-.1M12 0C8.741 0 8.332.015 7.052.072 5.771.129 4.807.308 4.042.633c-.78.337-1.44.787-2.102 1.45C1.278 2.747.828 3.407.49 4.187.166 4.952-.013 5.915-.07 7.196-.127 8.476-.142 8.885-.142 12c0 3.115.015 3.524.072 4.804.057 1.281.236 2.244.561 3.01.337.78.787 1.44 1.45 2.102.662.662 1.322 1.112 2.102 1.45.766.325 1.73.504 3.01.561 1.28.057 1.689.072 4.804.072s3.524-.015 4.804-.072c1.281-.057 2.244-.236 3.01-.561.78-.337 1.44-.787 2.102-1.45.662-.662 1.112-1.322 1.45-2.102.325-.766.504-1.73.561-3.01.057-1.28.072-1.689.072-4.804s-.015-3.524-.072-4.804c-.057-1.281-.236-2.244-.561-3.01-.337-.78-.787-1.44-1.45-2.102-.662-.662-1.322-1.112-2.102-1.45-.766-.325-1.73-.504-3.01-.561C15.524.015 15.115 0 12 0zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324zm0 10.162a3.999 3.999 0 1 1 0-8 3.999 3.999 0 0 1 0 8zm6.406-11.845a1.44 1.44 0 1 0 0-2.88 1.44 1.44 0 0 0 0 2.88z"/>
        </svg>
      </a>
    </div>
  </div>

  <div class="hero-right">
    <div class="hero-image">
      <!-- Note: You should replace this with a professional photo -->
      <img src="/PXL_20250206_162914543.MP.jpg" alt="Profile Photo" />
    </div>
  </div>

  <div class="scroll-indicator-container">
    <div class="scroll-indicator">
      <div class="mouse-shape">
        <div class="mouse-wheel"></div>
      </div>
      <span class="scroll-text">Scroll</span>
    </div>
  </div>

  <!-- Decorative elements -->
  <div class="decorative-shape shape-1"></div>
  <div class="decorative-shape shape-2"></div>
</section>

<script>
  // Smooth scroll function
  const scrollIndicator = document.querySelector(".scroll-indicator");
  const nextSection = document.querySelector(".next-section");
  
  if (scrollIndicator && nextSection) {
    scrollIndicator.addEventListener("click", function() {
      nextSection.scrollIntoView({ behavior: "smooth" });
    });
  }
  
  // Adding animation classes on load
  document.addEventListener("DOMContentLoaded", function() {
    const heroLeft = document.querySelector(".hero-left");
    const heroRight = document.querySelector(".hero-right");
    
    setTimeout(() => {
      if (heroLeft) {
        heroLeft.classList.add("animate-in");
      }
      if (heroRight) {
        heroRight.classList.add("animate-in");
      }
    }, 300);
  });
</script>

<style>
  /* HERO SECTION - ENHANCED */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    padding: 90px 10vw 2rem 10vw;
    background-color: #161616;
    position: relative;
    overflow: hidden; /* Ensure decorative elements don't cause overflow */
  }
  
  /* Decorative background elements */
  .decorative-shape {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.3;
    z-index: 0;
  }
  
  .shape-1 {
    width: 25vw;
    height: 25vw;
    background: radial-gradient(circle, rgba(211, 143, 65, 0.2), rgba(100, 21, 95, 0.05));
    top: -10%;
    left: -5%;
    animation: floatAnimation 15s infinite alternate ease-in-out;
  }
  
  .shape-2 {
    width: 20vw;
    height: 20vw;
    background: radial-gradient(circle, rgba(100, 21, 95, 0.15), rgba(211, 143, 65, 0.05));
    bottom: -5%;
    right: -2%;
    animation: floatAnimation 20s infinite alternate-reverse ease-in-out;
  }
  
  @keyframes floatAnimation {
    0% { transform: translate(0, 0); }
    100% { transform: translate(3%, 3%); }
  }

  .hero-left {
    flex: 1 1 60%;
    display: flex;
    position: relative;
    flex-direction: column;
    justify-content: center;
    min-width: 300px;
    z-index: 1;
    padding-right: 2rem;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }
  
  .hero-left.animate-in {
    opacity: 1;
    transform: translateY(0);
  }

  .name {
    font-family: 'Montserrat', sans-serif;
    font-size: 4.5rem;
    margin-bottom: 0.5rem;
    letter-spacing: 0.05rem;
    font-weight: 800;
    /* Improved animated gradient text */
    background: linear-gradient(270deg, #d38f41, #ff6600, #64155f);
    background-size: 400% 400%;
    animation: gradientAnimation 5s ease infinite;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: transparent;
    line-height: 1.1;
  }

  @keyframes gradientAnimation {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  .tagline {
    font-size: 1.7rem;
    margin-top: 0.5rem;
    font-weight: 400; /* Changed from 700 for a sleeker look */
    line-height: 1.4;
    margin-bottom: 2rem;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 0.05rem;
    animation: fadeInUp 1s ease-out 0.4s both;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
  }
  
  .tagline .highlight {
    font-weight: 700;
    position: relative;
    color: #ffffff;
    padding-right: 0.3rem;
  }
  
  .tagline .highlight::after {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, #d38f41, #64155f);
    transform-origin: left;
    transform: scaleX(0);
    animation: highlightLine 1s ease-out 1s forwards;
  }
  
  @keyframes highlightLine {
    from { transform: scaleX(0); }
    to { transform: scaleX(1); }
  }
  
  /* Use an em dash instead of a hyphen for better typography */
  .tagline::after {
    content: "";
    display: inline-block;
    margin: 0 0.3rem;
  }
  
  .name-spacing {
    display: inline-block;
    width: 0.5rem; /* Adjust this value to control the amount of space */
  }
  
  /* Update media queries for the tagline */
  @media (max-width: 1200px) {
    .tagline {
      font-size: 1.5rem;
    }
  }
  
  @media (max-width: 768px) {
    .tagline {
      font-size: 1.4rem;
      line-height: 1.3;
    }
  }
  
  @media (max-width: 480px) {
    .tagline {
      font-size: 1.25rem;
    }
  }
  
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .summary-wrapper {
    margin-top: 1rem;
    animation: fadeInUp 1s ease-out 0.8s both;
    position: relative;
  }
  
  .summary-container {
    position: relative;
    background: rgba(30, 30, 30, 0.5);
    border-radius: 12px;
    border-left: 3px solid #d38f41;
    padding: 1.5rem;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    max-width: 35rem;
  }
  
  .summary {
    position: relative;
    font-family: 'Montserrat', sans-serif;
    font-size: 1.5rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.85);
    margin: 0;
  }

  /* Link circles with icons under summary */
  .link-circles {
    margin-top: 2.5rem;
    display: flex;
    gap: 1.2rem;
    animation: fadeInUp 1s ease-out 1.2s both;
  }

  .circle-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 55px;
    height: 55px;
    border-radius: 50%;
    background-color: #955f21;
    transition: all 0.3s ease;
  }

  .circle-link:hover {
    background-color: #d38f41;
    transform: translateY(-5px);
    box-shadow: 0 5px 15px rgba(211, 143, 65, 0.4);
  }

  .circle-link svg {
    width: 30px;
    height: 30px;
    transition: transform 0.3s ease;
  }
  
  .circle-link:hover svg {
    transform: scale(1.1);
  }

  /* Right column: hero image */
  .hero-right {
    flex: 1 1 40%;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 300px;
    z-index: 1;
    opacity: 0;
    transform: translateX(20px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }
  
  .hero-right.animate-in {
    opacity: 1;
    transform: translateX(0);
  }

  .hero-image {
    position: relative;
    border-radius: 0.5rem;
    overflow: hidden;
    max-width: 90%;
    transition: transform 0.5s ease, box-shadow 0.5s ease;
  }
  
  .hero-image::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, rgba(211, 143, 65, 0.2), rgba(100, 21, 95, 0.1));
    pointer-events: none;
    opacity: 0.4;
    transition: opacity 0.5s ease;
  }
  
  .hero-image:hover {
    transform: translateY(-5px) scale(1.02);
    box-shadow: 0 15px 30px rgba(211, 143, 65, 0.5);
  }
  
  .hero-image:hover::after {
    opacity: 0.2;
  }

  .hero-image img {
    max-width: 100%;
    height: auto;
    display: block;
    border-radius: 0.5rem;
    box-shadow: 0 4px 20px rgba(211, 143, 65, 0.3);
  }
  
/* Complete replacement for the scroll indicator */
.scroll-indicator-container {
  position: fixed;
  bottom: 2rem;
  left: 0;
  right: 0;
  display: flex;
  justify-content: center;
  pointer-events: none;
  z-index: 10;
}

.scroll-indicator {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  pointer-events: auto;
  cursor: pointer;
  animation: fadeIn 1s ease-out 1.5s both;
}

.mouse-shape {
  width: 26px;
  height: 40px;
  border: 2px solid rgba(255, 255, 255, 0.7);
  border-radius: 20px;
  position: relative;
  margin-bottom: 8px;
}

.mouse-wheel {
  width: 4px;
  height: 8px;
  background-color: #d38f41;
  border-radius: 2px;
  position: absolute;
  top: 6px;
  left: 50%;
  transform: translateX(-50%);
  animation: mouseWheel 1.5s infinite;
}

@keyframes mouseWheel {
  0%, 100% {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
  50% {
    opacity: 0.5;
    transform: translateX(-50%) translateY(10px);
  }
}

.scroll-text {
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.8rem;
  letter-spacing: 0.05rem;
  text-transform: uppercase;
  font-weight: 500;
}

/* Simple, subtle hover effects without transforms */
.scroll-indicator:hover .mouse-shape {
  border-color: #d38f41;
}

.scroll-indicator:hover .scroll-text {
  color: #d38f41;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
  /* Responsive improvements */
  @media (max-width: 1200px) {
    .name {
      font-size: 3.8rem;
    }
    
    .tagline {
      font-size: 1.5rem;
    }
    
    .summary {
      font-size: 1.3rem;
    }
  }

  @media (max-width: 992px) {
    .hero {
      padding: 4rem 8vw;
    }
    
    .name {
      font-size: 3.5rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 6rem 5vw 4rem;
    }
    
    .hero-left, .hero-right {
      padding: 1rem 0;
    }
    
    .name {
      font-size: 3.2rem;
    }
    
    .tagline {
      font-size: 1.4rem;
      margin-bottom: 1.5rem;
    }
    
    .summary {
      font-size: 1.2rem;
    }
    
    .decorative-shape {
      opacity: 0.15;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 5rem 1.5rem 5rem;
    }
    
    .name {
      font-size: 2.8rem;
    }
    
    .tagline {
      font-size: 1.25rem;
    }
    
    .summary {
      font-size: 1.1rem;
    }
    
    .summary-container {
      padding: 1.2rem;
    }
    
    .link-circles {
      margin-top: 1.8rem;
    }
    
    .circle-link {
      width: 50px;
      height: 50px;
    }
    
    .circle-link svg {
      width: 25px;
      height: 25px;
    }
    
    .mouse-shape {
      width: 22px;
      height: 34px;
    }
  }
</style>